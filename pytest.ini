[pytest]
# ============================================================================
#  Pytest config para zc-automation-suite
# ============================================================================

# Requerido por pytest (en lugar del heredado setup.cfg)
minversion = 8.0

# Donde buscar tests
testpaths = tests

# Patrones de descubrimiento
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Opciones por defecto
addopts =
    -ra
    --strict-markers
    --strict-config
    --showlocals
    --tb=short

# Markers (registrados para evitar warning de --strict-markers)
markers =
    frontend_smoke: smoke tests del frontend (contract checks sobre .js)
    integration: integration tests (TestClient de FastAPI + Engine real, worker mockeado)
    e2e: smoke E2E con S7-1500 real (NO automatizado, ver .clinerules §12)

# asyncio mode (pytest-asyncio)
asyncio_mode = auto

# Paths (pytest 9.x: rootdir se detecta automaticamente del ini file;
# no es necesario declararlo. pythonpath lista los directorios que
# se anyaden a sys.path; "." es la raiz del proyecto.)
pythonpath =
    .
