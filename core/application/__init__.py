# Paquete ``core.application``: casos de uso / capa de aplicacion.
# Ver AGENTS.md: la separacion core/application vs core/web (interfaces)
# sigue Clean Architecture: ``application`` define los contratos
# (e.g. ``area_registry``); ``web`` los expone como HTTP.
